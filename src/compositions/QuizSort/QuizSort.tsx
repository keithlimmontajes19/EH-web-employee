import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const QuizSort = (props: PropsType): ReactElement => {
  return <>4</>;
};

export default QuizSort;
